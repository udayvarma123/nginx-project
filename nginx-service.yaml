apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: LoadBalancer
  selector:
    app: nginx
    color: blue   # default active environment
  ports:
  - port: 80
    targetPort: 80
